---
interface Props {
  href: string;
  extraClass?: string;
}

const { href, extraClass = "" } = Astro.props;
---

<a
  class={`underline hover:text-slate-600 hover:dark:text-slate-300 inline-block ${extraClass}`}
  href={href}
>
  <slot />
</a>
